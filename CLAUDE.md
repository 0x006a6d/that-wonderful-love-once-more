# CLAUDE.md

このリポジトリで作業するときに必ず読むこと。

## プロジェクト概要

Godot 4.x 製の3Dサードパーソン・アクションゲーム。籠城中の「ぷにけ銀行」に AIニケちゃんが介入し、犯人を制圧する。プレイヤーの行動によってエンディングが4分岐する。

**現在はコンテスト提出に向けた短期開発中。提出期限は 8/29。**

詳細設計は以下を参照する。

- `docs/game-design.md` — 世界観、物語構造、ゲームデザイン（全体像）
- `docs/technical-spec.md` — Godot上の実装仕様（全体像）
- `docs/tasks.md` — **日付入りの実装計画。コンテスト版に含める範囲はここが決定版**
- `docs/tasks-full.md` — 提出後に着手するフルスコープ版の計画
- `docs/permissions.md` — IP許諾の記録（リポジトリには含めない。ローカル管理）

**スコープは `tasks.md` の関門日で判断する。** 8/23 の縦切り完成を確認するまで、警察・銃モードといった追加機能に着手しないこと。8/28 以降は機能追加を一切行わず、バグ修正のみとする。開発者は Godot 未経験である前提で、エディタ上の操作が必要な箇所は手順を明示して人間に渡すこと。

## 絶対に守る制約

### 1. 実在事件への言及を作らない

本作は籠城・人質・警察突入という**ジャンルの定番構造**のみを使用する。実在の事件、実在の銀行、実在の人物、実在の地名、特定可能な年代を、コード・コメント・アセット名・UI文言・シナリオテキストのいずれにも登場させないこと。

- 銀行名は必ず「ぷにけ銀行」
- 舞台の年代・都市は特定しない
- 犯人は実在の誰かをモデルにしない

既存コードに実在事件を想起させる記述を見つけた場合は、修正して報告すること。

### 2. キャラクターIPの扱い

主人公 AIニケちゃん は第三者の公式IP。公式アセットは https://github.com/tegnike/nikechan-assets から取得する。二次創作ガイドライン詳細版: https://nikechan.com/guidelines/derivative

以下はガイドライン第3.3章（完成したデジタル作品の配布）ほかに基づく確定事項。**例外なく守ること。**

#### 配布条件

- **本作は無料配布のみ。** 課金、広告、投げ銭、寄付、支援プラン、任意支払い（Pay-what-you-want）に関するコードを実装しない。提案もしない
- 配布先は BOOTH / itch.io など価格0円に対応したプラットフォームとし、収益化機能はすべて無効化する
- 個人名義でリリースする。法人名義は不可

#### 表記（必須）

README および配布ページに以下を必ず記載する。この記載を削除・改変しない。

```
本作品は「AIニケちゃん」の非公式ファンメイド作品です。原作・公式とは関係ありません。
This is an unofficial fan-made work of AI Nike-chan. Not affiliated with or endorsed by the creator.
```

#### 使用できるアセット

使用可能なのは公式が許可した以下のみ。

- `vrms/` の VRMモデル3種
- `logos/` のロゴ画像
- 公式サイトで配布されている三面図

**上記以外の素材（既存イラスト、配信画像、スクリーンショット、他者の二次創作物など）を、生成AIの入力・参照・学習・変換に使わない。** これは学習だけでなく、Image-to-Image、スタイル転写、アップスケーリング、ポーズ参照といった推論時の変換にも適用される。画像・モーション・音声をAIで生成する必要が生じた場合は、実装せず報告すること。

#### 表現上の禁止事項

- 公序良俗に反する表現、差別的・反社会的な表現
- 本キャラクター、作者、第三者に対する誹謗中傷
- 公式・監修・公認と誤解させる表現、なりすまし
- 政治的・宗教的主張を含む内容
- 性的表現（本作では一切扱わない）

公式設定（年齢17歳、一人称「私」、敬語、イメージカラー #5A4C97）を勝手に改変しない。

#### 許諾状況

- **一般市民NPCへの攻撃メカニクス（`docs/game-design.md` §6.2、エンディング「逸脱」「失敗」分岐）は、作者から Discord にて許諾済み。** 仕様どおり実装してよい
- **主人公は一切発話しない。** ボイス、音声合成、発話テキストを実装しない。合成音声モデルは使用しないため、その申請も不要

承認の記録は `docs/permissions.md`（リポジトリには含めない。ローカル管理）に置く。承認範囲を超える追加要素（新たな表現、配布形態の変更など）を思いついた場合は、実装せず報告すること。

#### 改定への対応

ガイドラインは予告なく改定される。仕様書とガイドラインが矛盾する場合はガイドラインを優先し、報告すること。

### 3. 暴力表現の水準

犯人の無力化は「制圧」が基本。流血・損壊の描写は実装しない。ラグドールによるダウン表現までに留める。

## コーディング規約

- **GDScript**。C#は使わない
- **静的型付けを必須とする**。`var x := 0` / `func f(a: int) -> void:` の形式。型注釈のない変数・引数・戻り値を書かない
- ファイル名・ディレクトリ名は `snake_case`
- クラス名・ノード名は `PascalCase`
- シグナル名は過去形の `snake_case`（例: `civilian_downed`）
- 定数は `SCREAMING_SNAKE_CASE`
- 1ファイル1責務。300行を超えたら分割を検討する

## アーキテクチャ原則

- **グローバルな可変状態は `RunState` autoload のみ**。他のノードが状態を持ち回らない
- ノード間の直接参照（`get_node("../../")`）を書かない。シグナルか `@export` で注入する
- ゲームロジックとアニメーション再生を分離する。ステートマシンが `AnimationTree` を駆動し、逆方向の依存を作らない
- マジックナンバーを埋め込まない。調整値は `@export` にしてインスペクタから触れるようにする

## 作業の進め方

- `docs/tasks.md` のマイルストーンを**順番どおりに**進める。先のマイルストーンを先取りしない
- 各マイルストーンには受け入れ条件がある。満たしたら報告してから次に進む
- 不明点は推測で実装せず、質問する
- 1マイルストーンごとにコミットする。コミットメッセージは日本語で可

## 実行・確認

```bash
godot --path . --headless --quit          # プロジェクトが壊れていないか確認
godot --path . scenes/levels/bank_lobby.tscn   # 手動確認
```

エディタ上での目視確認が必要な作業は、確認手順を文章で提示して人間に委ねること。